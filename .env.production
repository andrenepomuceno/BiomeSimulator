# Production environment variables.
# This file is committed to the repository intentionally.
# The GA Measurement ID is public (visible in the rendered HTML to any visitor).
# To override without changing code, set GA_MEASUREMENT_ID as a GitHub Actions secret
# and pass it to the build step as VITE_GA_ID (see deploy-pages.yml).
VITE_GA_ID=G-5L3TW21MYM
